/**
 * Version accepted for every customer model upload. Increment whenever the
 * published conditions change, so an acceptance can always be tied to its text.
 */
export const UPLOAD_TERMS_VERSION = '2026-07-13'
